/* ============================================================
   BetterCalendar — config.js
   ──────────────────────────────────────────────────────────
   Only your OAuth Client ID is needed here — no API key required.

   ✅ Is it safe to put the Client ID in a public repo?
      YES. The Client ID for a web app OAuth flow is designed to
      be public. Google restricts it to only work from the
      "Authorised JavaScript origins" you set in Google Cloud
      Console (your GitHub Pages domain). Anyone who sees this
      file cannot use it from a different website.

   See README.md for step-by-step setup instructions.
   ============================================================ */

const CONFIG = {
  // OAuth 2.0 Client ID  (Web application type)
  // Looks like:  123456789-abc...xyz.apps.googleusercontent.com
  CLIENT_ID: '1075519823878-u0s3b6sjcskecka5acb01hhc68u47o2p.apps.googleusercontent.com',
};
